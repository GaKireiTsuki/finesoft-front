{
    "name": "finesoft-front-monorepo",
    "private": true,
    "type": "module",
    "scripts": {
        "ready": "vp fmt && vp lint && vp run -r build",
        "build:packages": "vp run -r build",
        "changeset": "changeset",
        "version": "changeset version",
        "release": "vp run -r build && changeset publish --access public",
        "release:auto:changeset": "node ./scripts/auto-changeset.mjs",
        "prepare": "vp config"
    },
    "devDependencies": {
        "@changesets/cli": "catalog:",
        "@vitest/coverage-v8": "catalog:",
        "typescript": "catalog:",
        "vite-plus": "catalog:"
    },
    "engines": {
        "node": ">=22.12.0"
    },
    "packageManager": "pnpm@10.33.0"
}
