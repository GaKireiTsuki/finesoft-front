# Application boundaries: local acceptance

Implementation revision: `faca935511a129cc4c4e9152de4328da9df3e033`; source tree `fe6d9e2c189babf822791a7fb1102986e219fe89`. Task7 review base: `a7a004f4386f74ac89102edea6c11c0f49c49094`. Whole-redesign cost baseline: `e22de6483b853af3e3e23c656a67af8c6e966994`. All matrix rows below apply to the implementation revision; the documentation-only completion commit does not change the runtime. Final independent Task7 and whole-branch reviews remain the parent reviewer's next step.

Every mandatory local matrix scenario has observable passing evidence. C2 passes the measurement/diagnosis requirement with real size and latency regressions; it is not a performance win. No push, publication, deployment, cloud upload or external message was performed.

## Runtime and artifact identity

macOS arm64; Node `v24.21.0`; Vite+ `0.2.8`; TypeScript `6.0.3`; pnpm11.20.0 via vp; Chrome `152.0.7977.83` (`channel: chrome`, headless), Playwright `1.63.0`; Miniflare `5.20260911.0-alpha`, actual workerd `1.20260911.1` without nodejs_compat. Workspace native peers: React19.2.8, Vue3.5.41, Svelte5.56.8. Packed Svelte-only fixture additionally checks5.55.1.

Packed `@finesoft/front@0.5.1` tarball SHA-256: `3e09f2207f68ac2771d5c908fe8e377648914c20da242d108fe747cff939ada6`. Exact entry/artifact/log hashes are in [final-artifacts.json](../reports/application-boundaries/final-artifacts.json); runtime metadata is in [final-runtime.json](../reports/application-boundaries/final-runtime.json). Reports and binary fixtures are local ignored evidence retained outside the temporary SDD directory; the architecture and this acceptance summary are committed.

## Joint matrix

| ID  | Scenario                                 | Status                | Final evidence                                                                                                                                                                                                                                                                                           |
| --- | ---------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| K1  | Typed operation: direct / HTTP / workerd | PASS                  | Core definitions/runtime and HTTP/Worker tests in coverage; portable runner; isolated packed positive/negative input/result checks. Same n=3→value=6 and denied category.                                                                                                                                |
| K2  | Independent and nested executions        | PASS                  | Core runtime/resources tests, concurrent HTTP/Worker binding tests; portable runner observes tenant-local values and protected nested call.                                                                                                                                                              |
| K3  | Async resources and cancellation         | PASS                  | Core resources/Container/http-signal, HTTP/Node/Worker tests: de-duplicated initialization, retry after rejection, awaited dependency-order disposal, same signal and cancellation blocking late commit.                                                                                                 |
| K4  | Queries and commands                     | PASS                  | Core runtime tests: explicit partitions/TTL/invalidation, policy before hit, captured query inputs, commands execute once and are neither cached nor implicitly retried. Abort has no rollback claim.                                                                                                    |
| H1  | Standard HTTP response                   | PASS                  | Portable runner consumes real Node and workerd Responses:201, two cookies, validation400, denied403, method rejection, redirect and safe fault; source tests cover header/body variants.                                                                                                                 |
| H2  | Streaming ownership                      | PASS                  | Real Node/workerd stream consumption plus HTTP/Node source tests for reader cancellation, failure, abort-bound fetch and request/host cleanup; response return is not resource disposal.                                                                                                                 |
| H3  | Exact generated module                   | PASS                  | generated-entry and ssr-handler tests parse/import/request emitted code with guard headers/status/redirect/locale/slots/request context; web-navigation runner emits real Node/prerender files; static-starter uses exported adapter.                                                                    |
| H4  | Host capabilities                        | PASS                  | secure-fetch/host-guard/client-ssrf and node/dns tests; real workerd has no nodejs_compat; packed root/http/worker graph has no Node/build/UI external; browser DNS policy is explicit.                                                                                                                  |
| W1  | One guarded page loader                  | PASS                  | Web definition/controller and SSR navigation/render tests exercise URL/SSR/tree parity, route-free targets and each Split target; supplied already-aborted cancellation regression fixed.                                                                                                                |
| W2  | Equal target, separate instances         | PASS                  | Web identity/keys/serialization tests plus native two-entry drafts and snapshot roundtrip; query identity does not replace EntryId.                                                                                                                                                                      |
| W3  | Transactions and history                 | PASS                  | Web controller and browser standard-start/navigation-bridge/History tests; latest URL admission, serialized tree edits, gated redirects, async popstate/scroll and final native matrix.                                                                                                                  |
| W4  | Persistent state                         | PASS                  | Async storage/session/snapshot/scoped-state and browser session/DOM tests; actual native hydration→restore, same-entry updates, type reset/remount, two-instance disposal and six-template reload.                                                                                                       |
| W5  | Public wire protocol                     | PASS                  | SSR materialization/strict/protocol and browser decoder tests; state-protocol runner builds twice, decodes paired producer/consumer, rejects old build and excludes nested secret.                                                                                                                       |
| B1  | Independent browser hosts                | PASS                  | Native runtime fixture: two explicit memory-history targets, separate language/state/session, address-bar owner rejection, one instance disposed while the other remains functional.                                                                                                                     |
| B2  | React / Vue / Svelte conformance         | PASS                  | Native 18-combination runner plus NATIVE_FIX_ONLY=1: mount/hydrate, draft-preserving same entry/type updates, type remount/reset, awaited ready/dispose and retained business slice.                                                                                                                     |
| B3  | Root/entries × SSR/CSR/prerender         | PASS                  | 18 native combinations, nested Svelte Tabs/Split/Stacks, six built templates and real staticAdapter-generated starter hydration; source mode/cache policies remain covered.                                                                                                                              |
| B4  | Actual browser Worker                    | PASS                  | Native runner builds adversarial browser-worker.ts, runs it in a real Chrome Worker, receives plain data/denied/bad-protocol messages and terminates worker.                                                                                                                                             |
| P1  | Installed packed consumers               | PASS                  | Six physically isolated temp installs: portable/no UI, React only, Vue only, Svelte only, Node only, tooling. Strict declarations without skipLibCheck/private source aliases; factory+handler page refs and operation negatives. Full six scaffold configs plus three isolated minimal installs/builds. |
| P2  | Entry dependency graphs                  | PASS                  | Packed runtime import/static+dynamic graph inspection; absent peer resolution is asserted from temp consumer. Browser/Worker lack Node/Vite; selected UI lacks other UIs; production Node lacks Vite/dotenv/UI. Tooling types/import work with selected peers.                                           |
| C1  | Ordinary integration                     | PASS                  | Four examples in architecture.md; six templates use one app declaration and native view registry. Typed page refs derive route/leaf/view association. Core plain-data and host examples exercised in packed/runtime fixture; native fixture validates second instance.                                   |
| C2  | Comparable costs                         | PASS with regressions | Final same-template build/source inventory, five fresh-process imports and 20-warmup/100-response SSR samples; module attribution, CPU diagnostic and two reverted lazy experiments retained. No performance improvement claimed.                                                                        |

## Commands and observed output

Commands run from the isolated application-boundaries worktree. Build and pack preparation mutations were sequential. Earlier pre-fix/lazy-experiment outputs are checkpoints; the named final logs below and final manifest identify the settled source. Source tests, compiled artifacts, installed artifacts and native runtimes are distinct evidence.

| Command                                                                           | Log                                                                                            | Observed result                                                                                           |
| --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `vp check`                                                                        | [task7-check.log](../reports/application-boundaries/task7-check.log)                           | 605 files formatted;488 typed/linted; no warnings/errors                                                  |
| `vp test --coverage`                                                              | [task7-coverage.log](../reports/application-boundaries/task7-coverage.log)                     | 109 files /831 tests; statements89.47%,branches83.36%,functions89.48%,lines91.60%                         |
| `vp run -r build`                                                                 | [task7-build-all.log](../reports/application-boundaries/task7-build-all.log)                   | 16/16 workspace builds; selected UI and docs built                                                        |
| `vp exec node scripts/verify-publish-transforms.mjs`                              | [task7-publish-transforms.log](../reports/application-boundaries/task7-publish-transforms.log) | Success/failure/duplicate prepack and exact restore pass                                                  |
| `vp exec node scripts/verify-runtime-boundaries.mjs`                              | [task7-packed.log](../reports/application-boundaries/task7-packed.log)                         | Six isolated installed consumers, types/imports/graphs pass                                               |
| `vp exec node scripts/verify-created-consumers.mjs`                               | [task7-created-consumers.log](../reports/application-boundaries/task7-created-consumers.log)   | Six independent generated configs; React/Vue/Svelte minimal installed and built outside workspace         |
| `vp exec node scripts/verify-web-package.mjs`                                     | [task7-web-package.log](../reports/application-boundaries/task7-web-package.log)               | Private core/Web ESM+CJS identity and NodeNext .d.mts/.d.cts checks pass                                  |
| `vp exec node scripts/verify-portable-runtime.mjs`                                | [task7-portable.log](../reports/application-boundaries/task7-portable.log)                     | Direct/Node/workerd contracts and9-module portable graph pass                                             |
| `vp exec node scripts/verify-native-renderers.mjs`                                | [task7-native.log](../reports/application-boundaries/task7-native.log)                         | 18 UI×strategy×mode combinations, nested Svelte structure,6 initial error cases and browser Worker pass   |
| `NATIVE_FIX_ONLY=1 vp exec node scripts/verify-native-renderers.mjs`              | [task7-native-fix.log](../reports/application-boundaries/task7-native-fix.log)                 | 9 groups:6 initial error/redirect cases and3 UI root reset/save/dispose/remount cases pass                |
| `vp exec node scripts/verify-template-renderers.mjs`                              | [task7-templates.log](../reports/application-boundaries/task7-templates.log)                   | All6 production templates, SPA/back/deep links/drafts/reload and Svelte locale/dev loader pass            |
| `vp exec node scripts/verify-static-starter.mjs`                                  | [task7-static-starter.log](../reports/application-boundaries/task7-static-starter.log)         | 3 adapter-generated HTML paths; real Chrome hydration/navigation/draft retention pass                     |
| `vp exec node scripts/verify-state-protocol.mjs`                                  | [task7-protocol.log](../reports/application-boundaries/task7-protocol.log)                     | Distinct paired builds, ready decode, stale build rejection, nested secret exclusion and Node output pass |
| `vp exec node scripts/verify-web-navigation.mjs`                                  | [task7-web-navigation.log](../reports/application-boundaries/task7-web-navigation.log)         | Built Node host; public prerender HTML exists; private artifact absent; hydration EntryId present         |
| `vp exec node scripts/verify-docs-demos.mjs`                                      | [task7-docs-demos.log](../reports/application-boundaries/task7-docs-demos.log)                 | EN/ZH Ch02 actual async routing and Ch03 guard short-circuit interactions pass                            |
| `vp exec node scripts/measure-application-boundaries.mjs`                         | [task7-costs.log](../reports/application-boundaries/task7-costs.log)                           | Settled source, all6 build assets, complete source inventory, imports and consumed SSR samples            |
| `vp exec node reports/architecture-baseline/measure-source-structure.mjs faca935` | [task7-source-costs.log](../reports/application-boundaries/task7-source-costs.log)             | All6 runtime workspace source trees counted against original baseline                                     |

Native result JSON/screenshots are in [native-renderers](../reports/native-renderers/results.json) and [fix-results](../reports/native-renderers/fix-results.json); six-template results are in [template-renderers](../reports/template-renderers/results.json). Exported static-adapter HTML, screenshot and result are under [static-starter](../reports/application-boundaries/static-starter/result.json). Packed consumer manifests, exact TS fixture source, install logs, asserted absent peers and per-entry graphs are under [packed](../reports/application-boundaries/packed/result.json). Temp installs lived under the OS temp directory, outside the repository and ancestor node_modules; they were deleted after checks.

## Costs

The same six template pages/functions and default production client build settings are compared. JS is the sum of all emitted client chunks; gzip compresses each chunk independently, not one artificial concatenation. Only Svelte-minimal defers locale chunks; its final initial static graph is112,438 raw /37,544 gzip bytes, versus total113,222 /38,149. The baseline full root and new portable root have different scope.

| Template       | Before JS | Final JS |    Δ JS | Before gzip | Final gzip | Δ gzip |
| -------------- | --------: | -------: | ------: | ----------: | ---------: | -----: |
| react          |   258,688 |  277,870 | +19,182 |      80,753 |     86,422 | +5,669 |
| react-minimal  |   257,622 |  274,838 | +17,216 |      80,597 |     85,772 | +5,175 |
| vue            |   131,025 |  154,857 | +23,832 |      45,833 |     53,405 | +7,572 |
| vue-minimal    |   134,296 |  152,031 | +17,735 |      47,584 |     52,872 | +5,288 |
| svelte         |   107,064 |  125,204 | +18,140 |      36,482 |     41,763 | +5,281 |
| svelte-minimal |    94,918 |  113,222 | +18,304 |      32,722 |     38,149 | +5,427 |

| Counted source          | Before files / lines | Final files / lines | Interpretation                                                                                                        |
| ----------------------- | -------------------: | ------------------: | --------------------------------------------------------------------------------------------------------------------- |
| Six-template assembly   |              20 /724 |             26 /427 | Includes main, SSR, bootstrap, app-definition, views, instance and old Svelte lib/render;297 fewer lines,6 more files |
| All six-template source |           105 /3,404 |          112 /3,196 | Includes relocated definitions and business code;208 fewer lines                                                      |
| Controllers             |              22 /745 |             22 /899 | 154 additional lines, including explicit public projections; not hidden as “framework savings”                        |
| App/view wrappers       |               9 /456 |              9 /371 | 85 fewer lines; business UI still exists                                                                              |
| Entire runtime source   |          119 /14,335 |         162 /15,215 | 43 more files and880 lines (+6.1%); includes Web/native/HTTP/resource capabilities                                    |

Counts include comments and blank lines, exclude tests/docs/build output/config, and do not measure cognitive complexity. [Source inventory](../reports/application-boundaries/costs/source-structure-final.json) records every file. The four onboarding exercises and their retained obligations are in [architecture.md](architecture.md#普通接入四个练习): page operation/route/view identity, explicit endpoint decode/encode, second target/history/stable persistence key, and host binding. Existing baseline could directly execute data but did not establish the new HTTP/lifetime contract; it had adapters but its exact generated module failed parsing. No fabricated old equivalent is used for new capabilities.

### Imports and request samples

Five fresh Node processes per import, with possibly warm OS caches. Baseline full root median7.612 ms and browser1.672 ms; final portable root1.943 ms, browser3.544 ms, and complete selected server (`index`+`web`+`ssr`+`node`)17.865 ms. Portable-root removal of environment modules is a scope reduction, not a same-function speedup. Complete server and browser imports are slower in these local samples.

SSR is the same built React-minimal home, local dispatch,20 warmups,100 samples, and complete `Response.text()` consumption. Baseline Hono `createSSRApp` median0.179417 /p950.315708 ms; final shared `createSSRHost` median0.295333 /p950.406166 ms. Median increases0.115916 ms (64.6%). **Both runs leave NODE_ENV unset; external React runs development code. These are local artifact microbenchmarks, not production capacity or network latency measurements.** The host/adapter changed, and a one-run sample is sensitive to GC/scheduling; raw samples and earlier checkpoints are preserved.

### Attribution and bounded adjustment

A metadata-only default Svelte-minimal rebuild records [built module contributions](../reports/application-boundaries/costs/client-modules.json). Major framework contributions before minification are Web navigation/session/shared loader66,270 bytes, browser startup/native integration/session34,864, and Core Runtime33,380. These are module rendered lengths, not an additive assignment of compressed-byte deltas. The same React/Vue/Svelte/Vite versions remain in the lockfile. The graph contains no unselected React/Vue, Node or Vite tooling module. Standard runtime now carries execution policies/resources, page identity/navigation snapshots and projection/version checks.

A20,000-request default1ms CPU-sampling diagnostic ([summary](../reports/application-boundaries/costs/profile-summary.json)) observes `serializeServerData`47 samples, `materializeServerData`8, snapshot decode/clone, execution creation, HTML injection, React work and GC. This supports the location of added work, not a precise phase-by-phase attribution of the timing difference. Required policy, ownership and public projection remain intact.

Two bounded lazy-loading experiments were measured and fully reverted. Session/DOM advanced public exports coalesced their support into the initial graph; the bundler explicitly reported the DOM dynamic import ineffective. The all-optional experiment reduced Svelte-minimal initial raw JS by909 bytes but increased emitted JS by1,737; its preserved build log shows approximately38.73KB initial /40.65KB total gzip, both worse than the pre-experiment37,539/38,144 bytes (the all-optional gzip values are rounded build-log sums; exact gzip bytes were not captured before reversion). The entries-only experiment reduced initial raw112,438→110,730, but initial independently gzipped bytes grew37,539→38,079; total113,222→113,878 raw and38,144→39,826 gzip, with an extra ~2.4KB entries request. Other templates gained only ~0.5KB initial raw and worsened initial gzip by ~1KB. Tiny gzip differences across paired build IDs are normal. [Experiment comparison and precision](../reports/application-boundaries/costs/lazy-experiment-summary.json), [All-optional modules](../reports/application-boundaries/costs/client-modules-lazy-all.json), [entries-only result](../reports/application-boundaries/costs/entries-only.json), and [pre-experiment checkpoint](../reports/application-boundaries/costs/pre-lazy.json) preserve the trial. Final dist contains no experimental imports/chunks. **Final session, DOM restoration and entries code remains statically reachable; initialization is conditional. No lazy-load or transfer improvement is claimed.** Further export/bundler redesign was outside this bounded adjustment.

## Fixes, refinements and remaining limits

- Root/subentry isolation, strict declaration consumers and exact publish restoration are implemented. Node production entry no longer re-exports the development server/Vite path. Optional UI peers are physically absent in unselected consumers; graph inspection alone is not the proof.
- `definePage` is a thin typed reference, not a new executor or view DSL. Handler/factory parameter and literal result typing is tested; widened BasePage, arbitrary codec-to-handler equivalence and native component prop validation are not promised.
- M1 strict serialization now remembers marked/unmarked materialization. Already-aborted supplied execution now receives cancellation before admission rejection; original signal, scope and owner are retained. RED coverage output is preserved as task7-coverage-red.log.
- Static adapter now uses built routes/shared SSR assembly and awaited cleanup, fails discovery/rendering errors, supports explicit dynamic/routesExport inputs, and rejects non200/redirect/runtime-only headers including Set-Cookie. Its generated HTML was hydrated in Chrome.
- Scaffolder output now has standalone tsconfig and selected Hono peer; create-app and runtime build configs use actual Vite+ pack configuration. Svelte templates have explicit config. AGENTS generated-marker damage was repaired; existing CI policy was documented without workflow edits.
- Current EN/ZH guides, templates and live demos use final imports and APIs. Ch02 handles asynchronous Router.resolve; both language route/guard demos were exercised. Historical plans/review are clearly marked historical, not rewritten as current instructions.
- Generated-install logs contain peer-range warnings with the intentionally pinned Vite alias (`@voidzero-dev/vite-plus-core@0.2.8`; public vite peer>=5). The installed tooling consumer has strict declarations and actual import/build passes. This metadata warning remains visible; no dependency upgrades were introduced.
- Adversarial fixture builds deliberately warn when Node-only test controllers are imported into their browser diagnostic graph. Ordinary public portable/browser graphs are separately verified clean. Local workerd does not validate Cloudflare deployment, and Chrome-only native evidence does not claim Firefox/WebKit conformance.

The [10 implementation rulings](../reports/application-boundaries/implementation-decisions.md) record choices and possible rework costs. All required local acceptance rows are verified; no external deployment/publication matrix is claimed. Independent review can still require corrections. Owned browsers, local servers and temporary installed consumers are closed/removed.
