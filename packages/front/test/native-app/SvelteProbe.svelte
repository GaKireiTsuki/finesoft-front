<script lang="ts">
import {getContext, onDestroy} from 'svelte';let {page,app}: {page:{title:string},app:{getSnapshot():{entries:readonly {page:{title?:string}}[]}}}=$props();let draft=$state('');const locale=getContext<{value:string}>('native-locale')??{value:'missing'};onDestroy(()=>{if(typeof window!=='undefined') (globalThis as any).cleanups=((globalThis as any).cleanups??0)+1});
</script><section data-restore-root data-context-locale={locale.value} data-snapshot-title={app.getSnapshot().entries.at(-1)?.page.title ?? "empty"}><h1>{page.title}</h1><input name="draft" bind:value={draft}/><span>{draft}</span></section>
