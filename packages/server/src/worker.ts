/** Portable HTTP execution already satisfies the Worker fetch contract. */
export {
    createHttpHandler,
    runManagedTask,
    type HttpHandler,
    type HttpHandlerOptions,
    type HttpHost,
    type ManagedTask,
} from "./http";
