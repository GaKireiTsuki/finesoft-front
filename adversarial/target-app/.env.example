# Server-side admin secret. Cookie `user=<this value>` is required to access /admin/secrets.
# Copy this file to .env and set a real value per deployment. Never commit the real .env.
ADMIN_TOKEN=changeme-set-me-in-env
